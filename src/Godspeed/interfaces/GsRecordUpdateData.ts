// @TODO note this is unused since GsRecordDataOps.update needs a complete record for now; maybe something like this will be more useful with an extracted data controller
export default interface GsRecordUpdateData {
	uuid: string;
	body: string;
}
